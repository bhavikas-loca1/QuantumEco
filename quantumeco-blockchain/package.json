{
  "name": "quantumeco-blockchain",
  "version": "1.0.0",
  "scripts": {
    "start:ganache": "ganache-cli --deterministic --accounts 10 --host 0.0.0.0 --port 8545",
    "migrate": "truffle migrate --reset",
    "test": "truffle test",
    "compile": "truffle compile"
  },
  "dependencies": {
    "@truffle/contract": "^4.7.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "ganache": "^7.9.0",
    "truffle": "^5.11.3",
    "truffle-plugin-verify": "^0.7.1"
  }
}
